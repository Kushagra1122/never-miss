# Copied to apps/mobile/.env — loaded when you run Expo from apps/mobile.
# Default: production API. For local API + OAuth, set EXPO_PUBLIC_API_URL to the
# same host as API_PUBLIC_URL in the repo root .env (e.g. LAN IP or nip.io).
EXPO_PUBLIC_API_URL=https://never-miss-api.onrender.com
